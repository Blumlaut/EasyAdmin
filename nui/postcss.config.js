// PostCSS config kept for tooling (stylelint, etc.)
// Tailwind CSS v4 is now handled by @tailwindcss/vite in vite.config.ts
// autoprefixer is no longer needed — v4 handles vendor prefixing automatically
export default {
  plugins: {},
}
